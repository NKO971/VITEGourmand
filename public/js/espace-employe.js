// Attendre que le DOM soit complètement chargé
document.addEventListener('DOMContentLoaded', () => {
    console.log("✅ Page employé chargée - Authentification gérée par le serveur PHP");
    
    // NOTE: Gestion des avis et litiges confiée au backend PHP
    // Les actions (valider, refuser, résoudre) POSTent directement au serveur
});

